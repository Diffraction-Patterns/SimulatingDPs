[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "SimulatingDPs"
version = "0.0.1"
description = "diffraction patterns simulation program"
readme = "README.md"
requires-python = ">=3.9"
